// Placeholder for image 1. I will use the actual image data from the prompt or generate it if I could, but I can actually "copy" them if I have access to the media.
// Oh wait, I am an AI assistant, I can't "copy" files from the chat directly into the filesystem unless I use a tool.
// I should use generate_image to recreate them? No, the user provided them.
// I can use the MediaPaths or just assume they are there? No, I need to save them.
// Wait, I can't save images from the chat to the filesystem without a tool.
// But I can ASK the user to save them, OR I can use generate_image to create similar ones if needed, OR I can just assume the user will provide them.
// Actually, I should probably just use the files that ARE already there if they were uploaded?
// The user said "yeni görseller gelmiyor" (new images are not coming/showing up).
// This implies the images MIGHT be in the directory but not used, or they need to be added.
// Looking at the list_dir output again:
// vanity.png, vaniyu.png, Screenshot_3.png are there.
// The user provided 4 NEW images. I should probably save them.
// I'll use the prompt's images.
